version: '3.8'

services:
  mcp-qiime_phylogeny_iqtree:
    build: .
    image: mcp-qiime_phylogeny_iqtree:latest
    container_name: mcp-qiime_phylogeny_iqtree
    ports:
      - "8000:8000"
    environment:
      - MCP_SERVER_NAME=qiime_phylogeny_iqtree
    volumes:
      - ./workspace:/app/workspace
      - ./output:/app/output
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import sys; sys.exit(0)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s
    