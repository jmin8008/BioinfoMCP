version: '3.8'

services:
  mcp-qiime_picrust2:
    build: .
    image: mcp-qiime_picrust2:latest
    container_name: mcp-qiime_picrust2
    ports:
      - "8000:8000"
    environment:
      - MCP_SERVER_NAME=qiime_picrust2
    volumes:
      - ./workspace:/app/workspace
      - ./output:/app/output
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import sys; sys.exit(0)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s
    