version: '3.8'

services:
  mcp-qiime_vsearch_cluster_features_de_novo:
    build: .
    image: mcp-qiime_vsearch_cluster_features_de_novo:latest
    container_name: mcp-qiime_vsearch_cluster_features_de_novo
    ports:
      - "8000:8000"
    environment:
      - MCP_SERVER_NAME=qiime_vsearch_cluster_features_de_novo
    volumes:
      - ./workspace:/app/workspace
      - ./output:/app/output
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import sys; sys.exit(0)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s
    