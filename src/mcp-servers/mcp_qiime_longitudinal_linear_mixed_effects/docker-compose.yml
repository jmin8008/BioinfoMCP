version: '3.8'

services:
  mcp-qiime_longitudinal_linear_mixed_effects:
    build: .
    image: mcp-qiime_longitudinal_linear_mixed_effects:latest
    container_name: mcp-qiime_longitudinal_linear_mixed_effects
    ports:
      - "8000:8000"
    environment:
      - MCP_SERVER_NAME=qiime_longitudinal_linear_mixed_effects
    volumes:
      - ./workspace:/app/workspace
      - ./output:/app/output
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import sys; sys.exit(0)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s
    