version: '3.8'

services:
  mcp-qiime_demux_summarize:
    build: .
    image: mcp-qiime_demux_summarize:latest
    container_name: mcp-qiime_demux_summarize
    ports:
      - "8000:8000"
    environment:
      - MCP_SERVER_NAME=qiime_demux_summarize
    volumes:
      - ./workspace:/app/workspace
      - ./output:/app/output
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import sys; sys.exit(0)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s
    